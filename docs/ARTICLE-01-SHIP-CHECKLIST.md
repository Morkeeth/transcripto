# ARTICLE-01 ship checklist — Oscar morning ticks

Cloud prepares. Oscar ticks. Agent does not post, publish, or bump PyPI.

Branch: `cursor/night-wave-p1-cold-verify-47f3` (tip = `git rev-parse --short HEAD`).
Artifact: `docs/COLD-VERIFY-2026-08-30.md`. Ruling:
`docs/STEP-3-README-BELOEVED-RULING.md`. Baseline: `docs/BASELINE-ARM.md`.

A checkbox is truth only when its done-when was RUN. Cloud ticks below name
the command. Oscar ticks stay unchecked.

---

## Cloud (done-when RUN tonight)

- [x] `hack.md` exists with seven parts — written before product/scripts code
      (`git show 64744a5:hack.md`).
- [x] `bash scripts/cold_verify.sh` → `cold_verify: PASS`
      (`TRANSCRIPTO_COLD_DIR=/tmp/transcripto-cold-final`, ~00:14Z UTC).
      Fixture ratio **504 of 2721**; oracle seed **20260919** → old30=**91**,
      old45=**46**; `blank_line_wc_trap: DETECTED`; stranger journey PASS;
      published verb gap DETECTED; tip stats caveat PRESENT.
- [x] `docs/COLD-VERIFY-2026-08-30.md` captures tonight's command output.
- [x] `bash scripts/archive_stranger.sh` → `cold_verify: PASS`.
- [x] `bash scripts/wheel_stranger.sh` → `wheel_stranger: PASS`
      (`cwd_shadow_trap: DETECTED`).
- [x] `bash scripts/cold_clone_stranger.sh` → `cold_verify: PASS`.
- [x] `bash scripts/pip_only_baseline.sh` → `pip_only_baseline: PASS`
      (`arm_a_ruling: naive find WINS retention`).
- [x] `./test_privacy_empty_index.sh` → `privacy_empty_index_watched_red: PASS`.
- [x] `python3 -m unittest discover -s tests -q` → **94 OK**.
- [x] STEP 3 ruling prepared at README L132–154 — Oscar tick open.
- [x] No outward acts from this agent (no article / X / PyPI).

## STEP 3 — README launch example (Oscar)

Cloud recommendation: **KEEP** invented demo at README.md:132–154.
See `docs/STEP-3-README-BELOEVED-RULING.md`.

- [ ] Oscar: keep invented replay demo (README.md:132–154) — recommended
- [ ] Oscar: or trim the demo further

## Oscar live corpus (OQ-1)

Cannot be done on this VM (no `~/.claude/projects`).

- [ ] Oscar: re-run find on live `~/.claude/projects`; publish **TZ**
- [ ] Oscar: refuse invented daily death rate from 504/2721 → 579/2874
- [ ] Oscar: name calendar midnight vs duration (`30*86400`) if either is quoted
- [ ] Oscar: note Desktop/Cowork mix — docs object says
      `desktopSessionCleanupPeriodDays` Default `0` (no age limit); live split
      is machine-local

## Oscar ship clicks (OQ-3)

- [ ] Article post
- [ ] X post
- [ ] Decide whether article waits on a PyPI cut that includes tip stranger
      verbs + stats caveat (live **0.2.0** lacks both — re-derived tonight)
- [ ] PyPI publish / version bump (agent does not)

---

## Footer — STEP 0 log (Slice 3)

```
$ ./test_small_n.sh 2>&1 | tail -5
  ok    raw survival line still shown

7/7 green.

$ bash scripts/test_small_n.sh 2>&1 | tail -5
  ok    raw survival line still shown

7/7 green.
```

Re-run at tip `d703142`, 2026-09-19 ~00:16Z UTC. Both paths **7/7**.
