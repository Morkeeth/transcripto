# STEP 3 — README beloeved / launch-example ruling

**For:** Oscar morning tick  
**Prepared:** 2026-09-23 (branch `cursor/night-wave-p1-launch-15b6`)  
**Object opened:** `README.md` via `nl -ba README.md | sed -n '128,160p'`

## What the object is tonight

The launch example under `## The replay` is the **invented** `replay --demo`
block. Line refs on this tip:

| lines | content |
|------:|---------|
| 130 | `## The replay` |
| 132–133 | Explicit: output from `replay --demo`; **All prompts and results … are invented.** |
| 135–154 | Fenced `text` demo: THE COMEBACK · failed/succeeded edits · recorded counts |
| 156–161 | Heading rules (comeback / snag / missing receipt / answer) — not grades |

Author “worst prompt” / beloeved personal text is **absent** on this tip
(scrubbed in earlier work). The surface Oscar decides on is the invented demo.

## Recommendation (cloud — not a tick)

**KEEP** the invented demo at `README.md:132–154`.

Reasons at the object:

1. It is labelled invented twice (L132–133) before any transcript text.
2. It teaches the evidence contract (FAIL/OK, source lines, “not task
   correctness”) without private words.
3. Trimming further would remove the only full-bleed example of replay
   headings a stranger can read without installing.

## Oscar ticks (do not agent-guess)

- [ ] **KEEP** invented replay demo (`README.md:132–154`) — recommended
- [ ] **TRIM** the demo further

Cloud does not tick either box.
