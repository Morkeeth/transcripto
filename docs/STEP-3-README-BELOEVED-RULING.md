# STEP 3 — README beloeved ruling (2026-09-15)

Oscar ticks KEEP or TRIM. Cloud does not guess the tick.

## Object opened tonight

```sh
nl -ba README.md | sed -n '130,154p'
```

Current beloved launch surface for the evidence contract is the **invented**
`replay --demo` block:

| lines | what |
|------:|------|
| 130–133 | "## The replay" + invented caveat |
| 135–154 | fenced demo: THE COMEBACK · FAIL/OK sequence · status caveat |

Author "worst prompt" / ranked-habit launch copy is **absent** from the README
object tonight (scrubbed earlier). Coach docs retain `best_prompt` /
`worst_prompt` only as null compatibility fields (L242) — not a launch table.

Secondary launch surface (not STEP 3's historical object, but shipped on main):

| lines | what |
|------:|------|
| 43–80 | stranger flow: `import-example` → `ask` → `changes` → `handoff` → `receive-handoff` |

Verified cold tonight inside `bash scripts/cold_verify.sh` →
`stranger_product_journey: PASS`.

## Recommendation (cloud)

**KEEP** the invented replay demo at `README.md:132–154`.

Reasons (evidence, not taste):
1. The demo is labelled invented and goes through the same parser path as real
   transcripts (`replay --demo`).
2. It teaches the evidence contract (FAIL/OK/unknown; status ≠ task correctness)
   without claiming Oscar's private history.
3. Trimming it leaves the stranger flow (L43–80) as the only first-read
   evidence story; that flow is real product value but does not show a
   comeback sequence at a glance.

## Oscar boxes (unticked)

- [ ] **Oscar:** keep invented replay demo (`README.md:132–154`) — recommended
- [ ] **Oscar:** or trim the demo further

Do not agent-tick.
